import { atom } from 'recoil';
import { v1 } from 'uuid';
/*
export const modalVisible = atom<boolean>({
  key: `modalVisible${v1()}`,
  default: false,
});

export const projectId = atom<number>({
  key: `projectId${v1()}`,
  default: -1,
});


 */
